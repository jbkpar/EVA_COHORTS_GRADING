spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grading
    username: root
    password: root

  jpa:
    show-sql: true   # <-- uses dash, not underscore
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, loggers, beans, env, info

server:
  port: 8080

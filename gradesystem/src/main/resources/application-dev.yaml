spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gradesystem
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9090

jwt:
  secret: fsdfihsgiha
